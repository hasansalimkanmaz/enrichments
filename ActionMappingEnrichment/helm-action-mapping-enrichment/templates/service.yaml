apiVersion: v1
kind: Service
metadata:
  name: {{ include "action-mapping-enrichment.fullname" . }}
  labels:
    {{- include "action-mapping-enrichment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "action-mapping-enrichment.selectorLabels" . | nindent 4 }}
