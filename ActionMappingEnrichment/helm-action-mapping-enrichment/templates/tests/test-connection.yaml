apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "action-mapping-enrichment.fullname" . }}-test-connection"
  labels:
    {{- include "action-mapping-enrichment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "action-mapping-enrichment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
